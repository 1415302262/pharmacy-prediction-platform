# Step 01 Audit

## 时间

2026-03-08

## 本步目标

盘点现有项目状态，确认上一版基线结果、代码结构与当前环境能力。

## 检查内容

- 检查项目目录、已有源码与已有图表
- 检查 `cptac` 环境中的可用库
- 读取上一版结果文件 `results/metrics_summary.csv`

## 结论

### 环境

- `rdkit`：可用
- `xgboost`：可用
- `catboost`：可用
- `torch`：可用
- GPU：可用
- `torch_geometric`：未安装，但当前阶段非必须

### 上一版最佳结果

- 最佳模型：`XGB_desc_fp`
- Test `R² = 0.6835`
- Test `RMSE = 0.6787`
- Test `MAE = 0.5075`

### 审核判断

上一版已具备基本 QSAR 展示能力，但仍有三方面不足：

1. 结构信息不够丰富；
2. 结构可视化不够多；
3. 尚未形成更强的集成模型。

因此决定在保持真实公开数据不变的前提下，做一轮“结构增强 + 模型增强 + 留痕增强”的升级。
